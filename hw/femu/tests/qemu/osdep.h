/*
 * Minimal stand-in so the NAND media layer builds outside QEMU. The media layer
 * needs nothing from QEMU proper -- see the Makefile for why that is worth it.
 */
#ifndef FEMU_TEST_OSDEP_H
#define FEMU_TEST_OSDEP_H
#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <string.h>
#endif
